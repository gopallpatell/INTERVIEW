────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 how internet works?
The Internet works by sending a request from the user (client) to a server where the data is stored. DNS converts the website name into an IP address, and using that IP 
address the request reaches the server. then The server responds by serving the requested data back to the client.” 
Ex.-
google.com->ISP->DNS->Server

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is a server ?
 A server is a computer or system that provides services, data, or resources to other computers (called clients) over a network or the internet.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is the diff between Server & Client 
Client is used to request data (files, images, services), and the server serves that data to the client.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is the difference between web server and application server ?
A web server serves static content, while an application server is where the application runs and processes business logic.
Web Server → HTML, CSS, JS, images (static files) -- nginx, apache
Application Server → Runs code, connects to database, generates dynamic data  -- NodeJs, Java Spring Boot
Web server delivers files, application server runs the application logic.

A web server handles client requests and serves static content, while an application server processes business logic and interacts with databases.
Web server = Files + Forward
App server = Logic + Database

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what it the diff btwn http and https and what is the port of both?
HTTP and HTTPS are application-layer protocols that define how a browser and a server communicate over the web, with HTTPS providing secure communication using encryption.
HTTP transfers data in plain text and is not secure; its default port is 80.
HTTPS encrypts data using SSL/TLS for security; its default port is 443.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is ssl and tls certificates.
SSL (Secure Sockets Layer):
SSL is the older protocol that was originally used to secure communications between web browsers and servers.
TLS Transport Layer Security is modern protocall it is more secure version of SSL.
SSL/TLS certificates help keep your data safe when browsing by securing the communication between your browser and a website.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 types of Applications
 there are mainly two types of application 
Standalone Application: Runs on a single system without internet, e.g., Calculator, MS word.
Web Application: Runs on a web browser and requires a server/network/database, e.g., Gmail, Facebook.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 why Linux used widely 
Linux is widely used because it’s free, secure, stable, fast, and ideal for servers and networking.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 components of Linux?
Kernel – Core of Linux, manages CPU, memory, and hardware.
Shell – Interface between user and kernel (command-line).
File System – Organizes files and directories.
Libraries – Pre-written code for programs to use.  
Utilities / Commands – Programs for tasks like copying, moving, or editing files.
Bootloader - Bootloader is the first program that starts the OS when a computer powers on.  GRUB, LILO
Bootloader runs first because it’s the “starter program” that loads the OS into the computer.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Stats of processes?
  running
  sleeping
  Stopped 
  killed
  terminating
  Zombie

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 to remove directory/folder
  rm -r cloud/
  rmdir cloud/

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 to print the first and last line from a file 
  first - head -n 5 file1
  last  - tail -n 5 file1 
  tail -f file1 - it will monitor the file which is  continuously updating like logs file so it will go inside the file and show us the continuously updating new lines.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'less' and 'more' command 
   both are commands are used to see the file page by page like if we have a log.txt file with 200 pages and we want to see the file page by page then we use less and more 
   command 
more /var/log/log.txt
Press Space → next page
Press Enter → next line
Press q → quit

less /var/log/log.txt
Press Space → next page
Press b → previous page
Press ↑ / ↓ → move line by line
Press /error → search word
Press q → quit

more moves only forward, while less allows both forward and backward navigation.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'cp' command 
 cp file1.txt devops/
 cp cloud/file1.txt devops/
 cp -r cloud/ devops/  #-r - recursively cp folder

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'mv' - to move and rename the file 
 mv file.txt devOps/     # to move the file forward
 mv file.txt ../cloud/   # to move the file backward .. is for backward directory
 mv devops/ Linux_for_devops  # rename
 mv file.txt file1.txt

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'wc'  
to see how many lines, words, and byte present in a file
wc file.txt   # it will work with multiple file in same command

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is hard link and soft link 
   Both hard link and soft link are used to link files. In a soft link, if the original file is deleted, the link breaks. But in a hard link, the file still exists even if 
   the original file is deleted.
ln -s file.txt softlink.txt     ln -s cloud/devops/file.txt softlink.txt
ln file.txt hardlink.txt        ln cloud/devops/file.txt hardlink.txt

ln → Hard link → data still accessible
ln -s → Soft link → breaks the link if original file is deleted and we cant access the data.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'cut'
 cut is used to give some portion or byte 
 cut 1 file.txt       # hello dosto   is the file content
 output - h
 cut 1-4 file.txt
 output - hello

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'tee'
tee command is used to create a file with content
echo "hello" | tee file.txt

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'sort'
it is used to rearrange the words and latter in alphabetical manner..

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'diff'
 this command is used to see the deference between two files or dir.
diff file.txt file1.txt 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 nohup?  No Hang Up
it is used to save the output of anything in the file that is "nohup.out"
like if we want to save the logs of any file into a file then we can use nohup <command>
 nohup top   nohup free -h   nohup df   nohup du 
 nohup command > output.log 2>&1 &

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'uname'  is used to see the operator/plateform  like linux/darwin(mac)

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'uptime'  used to see how long system has being running along with load and user..

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 diff in 'who' and whoami"
 "who" is used to see how many users loggin in this user, time when a user login to this system.
 "whoami" is used to see who is the current loged in user. 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'which' 
  this command is used to see the current path and current version of anything. like we can see where the tool is installed in which folder/dir.
  which java    which python  which docker   

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Package Management - Package Managers :::
				- Package Managers are used to install/Uninstall/Upgrade the Packages/software 
			
					- Centos / RHEL 		===> 	yum package Manager 
					- Debian / Ubuntu 		===>	apt/apt-get package Manager 
					- Fedora				===>	dnf (Latest Version of Fedora)
													yum (Previous Version of Fedora)

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'ctrl + r' it is used to search the command in cli 
  using this we can search the previously used commands         # to remove any tool -- sudo apt remove docker.io 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 to create and delete a user 
sudo useradd -m adminuser
sudo userdel adminuser

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 to create Group 
sudo groupadd devops      # it will create devops group
usermod -aG <group_name> <user_name>
usermod -aG docker devopsadmin

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 'chmod' is used to change the permissions of files

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is umask in Linux?
It decides which permissions should NOT be given when a new file or directory is created.
File default = 666 → rw-rw-rw-
Directory default = 777 → rwxrwxrwx

in linux umask is 0002    so permission is rwxrwxr-x
how to change the umask
Temporary       	umask 027
Permanent (user)	~/.bashrc     vi ~/.bashrc -> add at the end umask 027 -> source ~/.bashrc

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 "chown" It is used to change the owner and group of a file or directory.
chown [OPTIONS] owner:group file_name
for only user
chown user1 file.txt

only group
chown :devops file.txt

user and group
chown user1:devops file.txt

Change ownership recursively (directory + files)
chown -R user1:devops /project or file(file.txt)

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────       
🧠 What is zip?                                     # other tools gunzip,gzip
zip is a file compression tool.
It is used to combine multiple files/folders into one compressed .zip file to:
zip is a compression tool used to reduce file size and bundle files into a single archive.
- Save disk space
- Transfer files easily
- Backup data

to install zip --- sudo apt install zip unzip -y 

zip file.zip file.txt         zip files.zip file1.txt file2.txt
dir zip ---- zip -r folder.zip myfolder

unzip
unzip file.zip

-e Password protect zip
zip -e secure.zip file.txt

Check zip contents (without extracting)
unzip -l file.zip

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you compress and decompress files in Linux? 
  tar -czvf jayesh.tar.gz files
  tar -xzvf jayesh.tar.gz

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is LVM(logical volume manager)
Physical Volume (PV) → Each EBS volume you attach to the EC2 instance is treated as a physical volume in LVM.
sudo pvcreate /dev/xvdf   # xen virtual disk
sudo pvdisplay

Volume Group (VG) → Multiple PVs can be combined into a volume group, which acts like a storage pool. This allows you to 
manage multiple disks as a single unit.
sudo vgcreate vg_data /dev/xvdf
sudo vgdisplay

Logical Volume (LV) → From the volume group, you create logical volumes, which behave like normal partitions. You format them
with a filesystem and mount them for use.
sudo lvcreate -L 10G -n lv_data vg_data
sudo lvdisplay

EBS Volume → PV → combine PVs → VG → create LV → format & mount → usable storage

Create Filesystem on LV
sudo mkfs.ext4 /dev/vg_data/lv_data

mount
sudo mkdir /data
sudo mount /dev/vg_data/lv_data /data

real time use case is like when the attched volume is full and we need to extend the volume for that we use LVM For that we create new physical volume and attach it with VG
then we can add this in logical volume

EBS is created from the EC2 console by selecting volume type, size, and AZ, then attaching it to an EC2 instance in the same Availability Zone. or we can create it using CLI.

Normal mount can only use one device per path; to use a new EBS at the same path, copy that volume to a new location for backup then unmount then mount new one and then again
copy that backuped file in new mounted path. LVM allows dynamic expansion to include new EBS without unmounting.

A normal mounted path can only use the device it is mounted on.
/mnt/data is still using only the original 10 GB volume.
Adding a new EBS does not automatically merge with the old volume.
If you want to use the new EBS at the same mount point, you must do one of the

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you mount and unmount filesystems in Linux?
  mount is used to attach a storage device (like a hard drive, USB drive, or disk image) to a specific directory (mount point) in the filesystem.
  sudo mount /dev/sdb1 /mnt/mydrive
  umount is used to remove the device from the directory once you're done with it.
  sudo umount /mnt/mydrive

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 To stop/remove/delete more than one resource
 docker stop $(docker ps)
 docker rm $(docker ps -a)
 docker rm -f $(docker ps)
 docker rmi -f $(docker images)

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you troubleshoot network connectivity issues in Linux?
  Check Physical and Wi-Fi Connections: Ensure your device is properly connected to the network (Wi-Fi or Ethernet).
  Test Local Network (Router and Gateway): Verify your device can communicate with the router or gateway.
  Verify Internet Access: Try accessing websites to check if the internet is working.
  Check DNS Configuration: Ensure correct DNS settings to resolve domain names.
  Firewall or Security Software: Make sure firewalls or security software aren’t blocking internet access.
  ISP or External Issues: If nothing works, check if the issue is with your ISP or an external network problem.
  restart network: 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠  How do you list all the processes running in Linux?
  ps aux: Lists all processes running on the system with detailed information (like CPU and memory usage).
USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
root         1  0.0  0.1 169084  1348 ?        Ss   Jan04   0:02 /sbin/init
ec2-user   245  0.1  1.2 256432 12340 ?        S    10:12   0:01 nginx: worker
ec2-user   310  2.5  5.4 512000 54200 ?        Sl   10:20   0:45 java -jar app.jar

  top: Shows a real-time, continuously updating list of processes running on the system.
  htop: Similar to top, but with a more user-friendly, interactive interface (requires installation).
  pgrep -a <process_name>: Finds and lists all processes matching  specific name or pattern.
  In real time, pgrep -a is used to quickly verify whether a service is running, identify its PID, and see the exact command/arguments without using ps and grep.
  pgrep -a python       pgrep -a kubelet      pgrep -a containerd           pgrep -a java


1️⃣ Disk Full Issue (MOST COMMON ❗)
What you say 👇
One issue I resolved was the Linux root disk getting full, which caused the application to stop working.
How you explain (short & easy):
Problem: Server became unresponsive
Checked:
df -h
du -sh /var/log/*
Root Cause: Old log files consuming disk space
Fix:
journalctl --vacuum-time=7d         ##  removes system logs older than 7 days to free disk space.

Result: Disk space freed, application recovered

2️⃣ High CPU Usage by Application
What you say 👇
I faced a high CPU issue where a Java application was consuming most of the CPU.
Explanation:
Problem: Application was slow
Checked:
top
ps aux --sort=-%cpu
if the java application is used most of the cpu then we can check
pgrep -a java 
then find the PID of a stuck process kill -9 310

Root Cause: Application stuck in loop / heavy load
Fix: Restarted service and optimized resources
systemctl restart app
Result: CPU usage normalized

Server slow → ps aux --sort=-%cpu → PID 310 Java app 95% CPU → check logs → infinite loop → restart service → CPU normalized → applied CPU limit in Docker → system stable

3️⃣ Application / Service Down
What you say 👇
One time the application was not accessible because the service was down.
Explanation:
Checked:
systemctl status nginx
journalctl -u nginx      ### Useful when Nginx is not working or misbehaving.
pgrep -a nginx

Root Cause: Service failed after deployment
Fix: Restarted service and fixed config
Result: Application accessible again

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠  how can i check the configuration information in linux  
  lscpu: CPU architecture and details.
  free -h: Memory usage. 
  df -h: Disk usage.   
  ip a or ifconfig: Network interfaces.   
  top: Active processes.  
  whoami: Current user.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you find the process ID (PID) of a running process?
  pgrep <process_1>

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you change the password for a user account?
  passwd username 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the find command, and how do you use it?
  The find command in Linux is used to search for files and directories 
  find <directory> <options> <expression>
  find /path/to/search -type d -name "mydir"
  find /path/to/search -type f -name "*.jpg"

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is a swap space?   
  it is used as a virtual memory when the computers memory is fully utilizzed
  it is like a helper it will come in play when computer memory is full. it is not fast as regular memory

🧠 What are the environmental variables?
  Environment variables are predefined instructions like we have PATH env variable to define a path 
  then we have HOME to define where files are store we have USER like tht  we have lots of env variables.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What are the different modes when using the vi editor?
  Command Mode/ Regular Mode: Use the command Esc to enter Command Mode from any other mode.
  Insertion Mode/Edit Mode: You have to press i to enter Insertion Mode from Command Mode.
  Ex Mode/ Replacement Mode: You have to press the “:” (colon) to enter Ex Mode from Command Mode.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the ulimit command, and how do you use it?
  You can use the ulimit command to set the limit on the system resource to prevent consuming the higher resources. 
  This command contains multiple options to set the limit. For example, 
  you can use the u option to set a maximum number of processes to 50:
  ulimit -n 1000    # Set the maximum number of open files to 1000
  ulimit -u 200     # Set the maximum number of user processes to 200
  ulimit -u 50 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is grep:  command in linux 
  In short, grep is a searching tool that makes it easy to find text patterns in files and directories quickly and efficiently.
  Imagine you have a large document or a bunch of text files, and you want to find a specific word, phrase, or pattern. 
  Instead of manually searching, you can use grep to automatically find and display lines that match what you're looking for.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 🧠 Find the word "apple" in a file named fruits.txt:
  grep 'apple' fruits.txt

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is SMTP? 
  SMTP stands for Simple Mail Transfer Protocol. It is a standard protocol used to send emails across the internet. 
  Think of it as the "postman" of the internet, delivering your email messages from your computer to the recipient's mail server.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the difference between UDP and TCP?
  UDP (User Datagram Protocol) and TCP (Transmission Control Protocol) are both protocols used for sending data over the internet
  TCP is reliable, slow, and ensures data is delivered correctly.
  UDP is fast, but does not guarantee delivery or order, making it suitable for applications where speed is more important than accuracy (like live streaming or gaming).

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the difference between absolute and relative paths in Linux?
  Absolute Path
  Definition: An absolute path is the complete, full path from the root directory (/) to the file or directory. 
  Relative Path
  Definition: A relative path is the path relative to your current working directory.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you check the status of a service or daemon in Linux?
  sytemctl status apache2

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the difference between /etc/passwd and /etc/shadow files?
  The /etc/passwd file stores essential user information like usernames, user IDs, home directories, 
  and default shells. Each line in the file represents a user account.
  The /etc/shadow file contains encrypted passwords and other security-related information. 
  It is only accessible by the root user or privileged processes

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the sed command used for in Linux?
  sed commmand is used to replace edit delete insert any text in file without opening of that file. 
  In short, sed helps you make quick and automatic changes to text in files without needing to open them in an editor! 
  sed 's/old_text/new_text/' file
sed -i 's/8080/80/g' nginx.conf    ###  to change the ports
sed -n '3,6p' file.txt             ###  Print specific lines
sed '5d' file.txt                  ###  Delete a line from a file

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How to find and kill a process in Linux?
  ps aux | grep <process>
  kill <PID>
  If you don’t want to find the PID, then you can use the pkill command to kill a process by its name:
  pkill <process>

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the purpose of the SSH protocol in Linux, and how do you securely connect to a remote server using SSH?
  SSH the secure shell it is used to securely establish the connection bwn client server machine and the host machine 
  ssh username@remote_ip

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you check the contents of a file without opening it in Linux? 
  using cat command

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the purpose of the ping command in Linux, and how do you test network connectivity to a remote host?
  Ping command is used to test the network connectively between the local and remote hosts.
  ping remote_host_ip

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How can you find the IP address of a Linux system?
  The ‘ifconfig’ or ‘ip addr show’ command can be used to display the IP address of a Linux system.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Create a user and generate ssh key
  useradd devopsadmin -s /bin/bash -m -d /home/devopsadmin        ## -s = login shell     /bin/bash = default shell when user logs in
  ssh-keygen -t ecdsa -b 521	                                if dont use this then it used default /bin/sh  User can log in, 
                                                                                         but: Limited features
                                                                                         No .bashrc, .bash_profile
                                                                                         Less user-friendly                       

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is shell scripting and how can you run
  Shell scripting is a way to automate tasks in Linux by writing a series of commands in a plain text file.
  ./myscript.sh

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Download any package in the system using Linux
  sudo apt update 
  sudo apt install git

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is the wget command
  The wget command is a command-line utility used to download files from the web. 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 AWK command 
  awk is a command-line tool used to process and manipulate text by selecting 
  awk 'NR <= 10' filename   #This will print the first 10 lines of filename.
  awk '/hello/' filename    #This will print lines from filename that contain the string "hello".
  awk '!/error/' filename   #The !negates the pattern match, so this command prints lines that do not contain the word "error".

Prints the first column (USER)            ps aux | awk '{print $1}'
Prints USER and PID                       ps aux | awk '{print $1, $2}'

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What are Linux namespaces and cgroups?
  Namespaces isolate resources (like processes, network, filesystem) for different environments or containers.
  Cgroups control and limit resource usage (like CPU, memory) for groups 

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you use systemd in Linux?
  it is a service manager used to start stop restart enable disable the service using systemctl command line utitlity
  sudo systemctl stop nginx
  sudo systemctl restart nginx

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 how to automate the scipt in linux
   Cron is great for periodic tasks. 
   Open your terminal.
   Type crontab -e to edit your cron jobs.
   Add a new cron job. The basic syntax of a cron job is:
              * * * * * /path/to/your/script.sh
   At is useful for one-time tasks.
   echo "/path/to/your/script.sh" | at 10:00 AM

Logs fill up disk quickly.       ----- >>>    * * * * * /usr/local/bin/log_cleanup.sh
for backups                                            * * * * * /usr/local/bin/db_incremental_backup.sh

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 what is this in script "#!/bin/bash" 
  The line #!/bin/bash is called a shebang and is used at the beginning of a script to
  indicate which interpreter should be used to execute the script.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 how can i share my public key to remote machine in linux
   cat id_ecdsa.pub > authorized_keys
   scp /local/path/to/file username@remote_host:/remote/path/to/destination
   scp username@remote_host:/remote/path/to/file /local/path/to/destination

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 What is a firewall in Linux? -
A firewall is a security mechanism that controls incoming and outgoing network traffic based on a set of rules.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Server disk is full. How do you find the root cause?
Check disk usage using df -h       df -h output (filesystem usage): 
Filesystem      Size  Used  Avail  Use%  Mounted on
/dev/sda1        50G   42G    8G    84%   /
tmpfs            2G     0     2G     0%   /run

Identify large directories with du -sh /*
du -sh output (directory usage):
3.2G   /var/log

Look for common issues: logs (/var/log), Docker data (/var/lib/docker), temp files
Check deleted but open files using lsof | grep deleted
✅ Conclusion: Disk usually fills up due to logs, Docker images/containers, or uncleaned files.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 Application is slow on server. What will you check?
CPU usage → top, htop
Memory usage → free -h
Disk usage / I/O → df -h, iostat
Network latency → ping, netstat
Application logs → check for errors or timeouts
Running processes → ps, top
Database performance → slow queries, connection issues

✅ Conclusion: Slowness is usually caused by high CPU, low memory, disk I/O wait, network delay, or DB issues.
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you debug a crashed service?
Check service status
systemctl status <service-name>

Check logs for errors
journalctl -u <service-name>

Verify configuration files for mistakes
Check resource usage (CPU, memory, disk)
Confirm dependencies (DB, network, ports)
Restart the service and monitor
systemctl restart <service-name>

✅ Conclusion: Most crashes are due to config errors, missing dependencies, or resource exhaustion.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you check which process is using a port?
When a service fails to start, it’s often because the port is already in use by another process.

1. Using ss
ss -tulnp | grep :8080
t → TCP
u → UDP
l → listening ports
n → show port numbers
p → show process (PID & name)

📌 Output tells you which process and PID is using the port.

2. Using netstat
netstat -tulnp | grep :8080
Shows the same info (older but still common).

3. Using lsof
lsof -i :8080
Lists the process holding the port.

Example output
java   2345  user   123u  TCP *:8080 (LISTEN)
➡️ Java process with PID 2345 is using port 8080.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you rotate logs?
Log rotation is done using logrotate.
It automatically rotates, compresses, and deletes old logs based on size or time to prevent disk full issues.
Create a logrotate config

Create a file:
sudo vi /etc/logrotate.d/myapp
Add this:
/var/log/myapp.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
}

What each line means
daily → rotate logs every day
rotate 7 → keep last 7 log files
compress → old logs become .gz
missingok → no error if log file is missing
notifempty → don’t rotate if file is empty

What happens after rotation
myapp.log        → current log (new file)
myapp.log.1      → yesterday’s log
myapp.log.2.gz   → older compressed log

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠 How do you debug high memory usage?

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
🧠

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
